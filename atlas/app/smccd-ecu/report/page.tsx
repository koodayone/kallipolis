import ClientPage from "./ClientPage";

// Energy, Construction & Utilities report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdEcuReportPage() {
  return <ClientPage />;
}
