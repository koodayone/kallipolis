import ClientPage from "./ClientPage";

// ICT / Digital Media report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdIctReportPage() {
  return <ClientPage />;
}
