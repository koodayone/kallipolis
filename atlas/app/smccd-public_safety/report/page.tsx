import ClientPage from "./ClientPage";

// Public Safety report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdPublicSafetyReportPage() {
  return <ClientPage />;
}
