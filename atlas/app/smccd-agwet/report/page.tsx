import ClientPage from "./ClientPage";

// Ag, Water & Environmental Technologies report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdAgwetReportPage() {
  return <ClientPage />;
}
