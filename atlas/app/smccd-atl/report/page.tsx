import ClientPage from "./ClientPage";

// Advanced Transportation & Logistics report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdAtlReportPage() {
  return <ClientPage />;
}
