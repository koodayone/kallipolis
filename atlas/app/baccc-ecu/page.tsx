import LandscapeDashboard from "@/college-atlas/partnerships/LandscapeDashboard";

// BACCC × ecu — the Bay Area Community College Consortium sector surface (all
// 26 Bay colleges), parameterized by the `baccc-ecu` landscape instance.
export default function BacccEcuPage() {
  return <LandscapeDashboard instance="baccc-ecu" />;
}
