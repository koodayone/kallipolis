import ClientPage from "./ClientPage";

// The SVAMP report — the narrative, citeable surface. The dashboard owns the
// root (/svamp); this is its descent, one click away via SurfaceNav with the
// selection preserved through the shared svampUrl params.
export default function SvampReportPage() {
  return <ClientPage />;
}
