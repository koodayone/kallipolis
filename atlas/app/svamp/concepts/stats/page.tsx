import StatsConcepts from "@/college-atlas/partnerships/StatsConcepts";

// Design exploration for the dashboard's demand-stats panel — sibling of
// /svamp/concepts; delete together once the panel is settled.
export default function SvampStatsConceptsPage() {
  return <StatsConcepts />;
}
