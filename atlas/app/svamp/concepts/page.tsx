import DashboardConcepts from "@/college-atlas/partnerships/DashboardConcepts";

// Design exploration for the SVAMP dashboard layout forks — a temporary
// surface for judging layout options in the product's own design language.
// Remove together with DashboardConcepts.tsx once the design is settled.
export default function SvampDashboardConceptsPage() {
  return <DashboardConcepts />;
}
