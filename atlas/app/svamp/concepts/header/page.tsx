import HeaderConcepts from "@/college-atlas/partnerships/HeaderConcepts";

// Design exploration for the dashboard header language — sibling of
// /svamp/concepts; delete together once the design is settled.
export default function SvampHeaderConceptsPage() {
  return <HeaderConcepts />;
}
