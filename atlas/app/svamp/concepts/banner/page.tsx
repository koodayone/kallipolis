import BannerConcepts from "@/college-atlas/partnerships/BannerConcepts";

// Design exploration for the dashboard's scope banner — sibling of
// /svamp/concepts; delete together once the banner is settled.
export default function SvampBannerConceptsPage() {
  return <BannerConcepts />;
}
