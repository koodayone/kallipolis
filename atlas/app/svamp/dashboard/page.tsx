import SvampDashboard from "@/college-atlas/partnerships/SvampDashboard";

// The SVAMP report transposed to a strict-fit dashboard — see the design
// record at /svamp/concepts and the requirements docstring in SvampDashboard.
export default function SvampDashboardPage() {
  return <SvampDashboard />;
}
