import SvampDashboard from "@/college-atlas/partnerships/SvampDashboard";

// BACCC × ict — the Bay Area Community College Consortium sector surface (all
// 26 Bay colleges), parameterized by the `baccc-ict` landscape instance.
export default function BacccIctPage() {
  return <SvampDashboard instance="baccc-ict" />;
}
