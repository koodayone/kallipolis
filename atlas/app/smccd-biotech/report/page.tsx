import ClientPage from "./ClientPage";

// The SMCCD Life Sciences / Biotech report — the sector-derived instance's
// narrative surface, mirroring the other landscapes' /report. The dashboard owns
// the root (/smccd-biotech); this is its descent, one click away via SurfaceNav
// with the selection preserved through the shared landscapeUrl params.
export default function SmccdBiotechReportPage() {
  return <ClientPage />;
}
