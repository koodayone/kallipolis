import ClientPage from "./ClientPage";

// The SMCCD Life Sciences & Health Technology report — instance #3's narrative
// surface, mirroring /svamp/report and /smccd/report. The dashboard owns the
// root (/smccd-health); this is its descent, one click away via SurfaceNav with
// the selection preserved through the shared landscapeUrl params.
export default function SmccdHealthReportPage() {
  return <ClientPage />;
}
