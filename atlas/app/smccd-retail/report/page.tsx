import ClientPage from "./ClientPage";

// Retail, Hospitality & Tourism report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdRetailReportPage() {
  return <ClientPage />;
}
