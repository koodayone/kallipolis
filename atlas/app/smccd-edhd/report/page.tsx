import ClientPage from "./ClientPage";

// Education & Human Development report — the sector instance's narrative surface, mirroring the other
// landscapes' /report (one click from the dashboard via SurfaceNav).
export default function SmccdEdhdReportPage() {
  return <ClientPage />;
}
