import LandscapeDashboard from "@/college-atlas/partnerships/LandscapeDashboard";

// BACCC × health — the Bay Area Community College Consortium sector surface (all
// 26 Bay colleges), parameterized by the `baccc-health` landscape instance.
export default function BacccHealthPage() {
  return <LandscapeDashboard instance="baccc-health" />;
}
